import React from 'react';
// import Section from './navContext';



export default function NavLink(props){

    return(                         
            <>
                <div id="main">
                    {props.children}
                </div>
            </>          
    );
}